{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start App Server",
            "type": "shell",
            "command": "anvil-app-server --app ${workspaceFolder} --dep-id C6ZZPAPN4YYF5NVJ=Anvil_Extras --auto-migrate"
        }
    ]
}